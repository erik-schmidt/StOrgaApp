import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  boldText: {
    fontWeight: "bold",
  },
  courseHeader: {
    fontWeight: "bold",
    fontSize: 20
  }
});

export default styles;
